import { ReactElement } from "react";
import { Page } from "../SharedComponents/Page/Page";

export const DiceRoller = (): ReactElement => {
    return <Page title="Dice Roller">Dice roller stuff!</Page>;
};
